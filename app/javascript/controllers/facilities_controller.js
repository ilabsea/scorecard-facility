import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  delete(event) {
    // let confirmed = confirm('Are you sure to delete this record?')

    // if(!confirmed) {
    //   event.preventDefault();
    // }
  }
}
