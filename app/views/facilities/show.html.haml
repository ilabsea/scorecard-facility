= render "shared/modals/remote_modal", title: t('shared.confirm_delete', name: t('facility.facility')) do

  %div
    = t('shared.are_you_sure_to_delete', type: t('facility.facility'), name: @facility.name).html_safe

  .d-flex.justify-content-end.mt-3
    = render "shared/buttons/modal_cancel_button"
    = render "shared/buttons/modal_delete_button", item: @facility
